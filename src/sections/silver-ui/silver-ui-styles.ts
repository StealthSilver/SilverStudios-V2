/**
 * @file silver-ui-styles.ts
 * @description Shared flags and layout class strings for the Silver UI section.
 */

/** Temporarily hide the Silver UI section on the page (code kept for later). */
export const SILVER_UI_SECTION_HIDDEN = true;
