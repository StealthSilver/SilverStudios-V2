/**
 * @file site.ts
 * @description Site-wide copy and metadata — never hardcode in JSX.
 */

import type { SiteConfig } from "@/types";

export const siteConfig: SiteConfig = {
  name: "Silver Studios",
  tagline: "Design, motion, and digital craft.",
  description:
    "Silver Studios builds thoughtful digital experiences — from brand systems to interactive product UI.",
};
